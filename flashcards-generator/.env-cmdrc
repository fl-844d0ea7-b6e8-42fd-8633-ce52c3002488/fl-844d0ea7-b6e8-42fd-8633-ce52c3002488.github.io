{
    "debug": {
        "FLASHCARDS_VAULT_HOSTNAME": "http://localhost:3001",
        "NODE_ENV": "development",
        "BABEL_ENV": "client"
    },
    "dev": {
        "NODE_ENV": "production",
        "BABEL_ENV": "client",
        "FLASHCARDS_VAULT_HOSTNAME": ${{ secrets.DEV_FLASHCARDS_VAULT_HOSTNAME }}
    }
}